---
import Layout from '../layouts/Layout.astro';
---


<Layout>
  <!-- Header -->
  <header class="fixed top-0 left-0 right-0 z-50 bg-transparent py-6 px-8 backdrop-blur-lg">
    <nav class="max-w-7xl mx-auto flex justify-between items-center text-white">
      <div class="text-2xl font-bold">
        <a href="#">Mi Portafolio</a>
      </div>
      <ul class="flex space-x-8 text-lg">
        <li><a href="#sobre-mi" class="hover:text-gray-300 transition-all">Sobre mí</a></li>
        <li><a href="#habilidades" class="hover:text-gray-300 transition-all">Habilidades</a></li>
        <li><a href="#contacto" class="hover:text-gray-300 transition-all">Contacto</a></li>
      </ul>
    </nav>
  </header>

  <!-- Hero Section -->
  <section class="flex items-center justify-center min-h-screen bg-gradient-to-r from-purple-600 to-blue-600 text-center text-white relative overflow-hidden">
    <!-- Floating Circles with animations -->
    <div class="absolute top-1/4 left-1/4 w-32 h-32 bg-blue-500 rounded-full opacity-30 animate-float delay-2s"></div>
    <div class="absolute top-2/3 left-1/3 w-48 h-48 bg-purple-500 rounded-full opacity-30 animate-float delay-3s"></div>
    <div class="absolute top-1/2 right-1/4 w-40 h-40 bg-pink-500 rounded-full opacity-20 animate-float delay-4s"></div>

    <!-- Content -->
    <div class="z-10 space-y-8">
      <h1 class="text-5xl font-bold sm:text-6xl animate__animated animate__fadeIn">Soy Desarrollador Web</h1>
      <p class="text-lg sm:text-xl animate__animated animate__fadeIn animate__delay-1s">Transformo ideas en experiencias digitales únicas y funcionales.</p>
      <a href="#contacto" class="px-8 py-4 text-xl bg-white text-black rounded-lg hover:bg-gray-200 hover:scale-105 transition-all duration-300 animate__animated animate__fadeIn animate__delay-2s">Contáctame</a>
    </div>
  </section>

  <!-- Sobre mí -->
  <section id="sobre-mi" class="py-24 bg-black text-white text-center">
    <div class="max-w-4xl mx-auto">
      <h2 class="text-3xl font-bold mb-6 animate__animated animate__fadeIn">Sobre mí</h2>
      <p class="text-lg animate__animated animate__fadeIn animate__delay-1s">Soy un desarrollador web apasionado por crear soluciones visuales atractivas y funcionales. Mi enfoque es siempre en la experiencia del usuario y en optimizar cada detalle técnico.</p>
    </div>
  </section>

  <!-- Habilidades -->
  <section id="habilidades" class="py-24 bg-gray-900 text-white">
    <div class="max-w-7xl mx-auto text-center">
      <h2 class="text-3xl font-bold mb-8 animate__animated animate__fadeIn">Mis Habilidades</h2>
      <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-8">
        <div class="transform hover:scale-105 hover:shadow-2xl transition-all duration-300 p-8 rounded-xl bg-gradient-to-br from-purple-500 via-blue-500 to-pink-500 hover:from-purple-600 hover:via-blue-600 hover:to-pink-600">
          <h3 class="text-xl font-semibold">HTML & CSS</h3>
          <p class="mt-2">Creación de interfaces web responsivas y accesibles.</p>
        </div>
        <div class="transform hover:scale-105 hover:shadow-2xl transition-all duration-300 p-8 rounded-xl bg-gradient-to-br from-blue-500 via-teal-500 to-green-500 hover:from-blue-600 hover:via-teal-600 hover:to-green-600">
          <h3 class="text-xl font-semibold">JavaScript</h3>
          <p class="mt-2">Interactividad dinámica con lógica en aplicaciones web.</p>
        </div>
        <div class="transform hover:scale-105 hover:shadow-2xl transition-all duration-300 p-8 rounded-xl bg-gradient-to-br from-teal-500 via-green-500 to-yellow-500 hover:from-teal-600 hover:via-green-600 hover:to-yellow-600">
          <h3 class="text-xl font-semibold">React & Tailwind CSS</h3>
          <p class="mt-2">Desarrollo frontend eficiente con componentes reutilizables.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Contacto -->
  <section id="contacto" class="py-24 bg-gradient-to-r from-blue-600 to-purple-600 text-white text-center">
    <div class="max-w-2xl mx-auto">
      <h2 class="text-3xl font-bold mb-6 animate__animated animate__fadeIn">¿Trabajemos juntos?</h2>
      <p class="text-lg mb-8 animate__animated animate__fadeIn animate__delay-1s">Si estás interesado en colaborar o necesitas más información, no dudes en contactarme.</p>
      <a href="mailto:joscalde13@gmail.com" class="inline-block px-8 py-4 text-xl bg-white text-black rounded-lg hover:bg-gray-200 hover:scale-105 transition-all duration-300 animate__animated animate__fadeIn animate__delay-2s">Envíame un correo</a>
    </div>
  </section>

  <!-- Footer -->
  <footer class="py-6 text-center text-white bg-black">
    <p>&copy; 2025 Mi Portafolio - Todos los derechos reservados.</p>
  </footer>

  <!-- Animaciones -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
  <style>
    @keyframes float {
      0% { transform: translateY(0); }
      50% { transform: translateY(-15px); }
      100% { transform: translateY(0); }
    }

    .animate-float {
      animation: float 3s ease-in-out infinite;
    }

    /* Cursor personalizado */
    #cursor {
      position: fixed;
      width: 20px;
      height: 20px;
      background-color: rgba(255, 255, 255, 0.8);
      border-radius: 50%;
      pointer-events: none;
      transform: translate(-50%, -50%);
      transition: width 0.3s ease, height 0.3s ease;
    }

    .cursor-hover {
      width: 50px;
      height: 50px;
    }
  </style>

  <!-- Cursor JS -->
  <script>
    const cursor = document.createElement('div');
    cursor.id = 'cursor';
    document.body.appendChild(cursor);

    document.addEventListener('mousemove', (e) => {
      cursor.style.left = `${e.pageX}px`;
      cursor.style.top = `${e.pageY}px`;
    });

    const hoverElements = document.querySelectorAll('a, button');
    hoverElements.forEach(el => {
      el.addEventListener('mouseenter', () => {
        cursor.classList.add('cursor-hover');
      });
      el.addEventListener('mouseleave', () => {
        cursor.classList.remove('cursor-hover');
      });
    });
  </script>
</Layout>
